2.2 PL/SQL PROGRAMS-FUNCTIONS

1. Create a function to calculate price_range(low,medium,high) of each product. If price<=5 indicate as low,upto 50 indicate as medium,and above as high.
mysql> DELIMITER //
mysql>
mysql> CREATE FUNCTION price_range(p INT)
    -> RETURNS VARCHAR(10)
    -> DETERMINISTIC
    -> BEGIN
    ->     DECLARE result VARCHAR(10);
    ->
    ->     IF p <= 5 THEN
    ->         SET result = 'Low';
    ->     ELSEIF p <= 50 THEN
    ->         SET result = 'Medium';
    ->     ELSE
    ->         SET result = 'High';
    ->     END IF;
    ->
    ->     RETURN result;
    -> END //
Query OK, 0 rows affected (0.01 sec)

mysql> DELIMITER ;
mysql> SELECT pname, price, price_range(price)
    -> FROM product;
+--------+-------+--------------------+
| pname  | price | price_range(price) |
+--------+-------+--------------------+
| Pen    | 10.20 | Medium             |
| Book   | 51.00 | High               |
| Pencil | 25.50 | Medium             |
+--------+-------+--------------------+
3 rows in set (0.00 sec)